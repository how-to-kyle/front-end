import React from 'react';

const HowToCard = props => {
  // const { title, director, metascore, stars } = props.howTo;
  console.log(props);

  return (
    <div className="howToCard">     
    </div>
  );
};

export default HowToCard;
